@model List<GroceryManagement.Models.LeaveRequest>
@{
    ViewBag.Title = "Leave Approvals";
    var info = TempData["Info"] as string;
    var managerId = ViewBag.ManagerId as string ?? "";
}

<h2>Leave Approvals</h2>

<p><strong>Manager ID:</strong> @managerId</p>

@if (!string.IsNullOrWhiteSpace(info))
{
    <div class="alert">@Html.Raw(info)</div>
}

<div id="target">
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Staff</th>
                <th>Date</th>
                <th>Type</th>
                <th>Status</th>
                <th>Submitted</th>
                <th>Approved By</th>
                <th>Attachment</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @Html.Partial("_LeaveApprovalsTable", Model)
        </tbody>
    </table>
</div>
